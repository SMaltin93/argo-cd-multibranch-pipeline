apiVersion: apps/v1
kind: Deployment
metadata:
  name: web-app-dev
  namespace: python-flask
spec:
  replicas: 2
  selector:
    matchLabels:
      app: web-app
  template:
    metadata:
      labels:
        app: web-app
    # spec:
    #   containers:
    #   - name: web-app
    #     image: tacksam/flask-app:latest  # Use the latest tag or SHA for the new image
    #     imagePullPolicy: Always           # Ensure Kubernetes pulls the latest image
    #     ports:
    #     - containerPort: 5000
